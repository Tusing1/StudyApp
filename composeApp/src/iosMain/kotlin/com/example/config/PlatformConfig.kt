package com.example.config

// For iOS, the API key should ideally be injected securely or read from Info.plist.
// For now, we will return a placeholder, or you can supply it during initialization.
actual val GEMINI_API_KEY: String = "MY_GEMINI_API_KEY"
